---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.jsx';
import Services from '../components/Services.jsx';
import ContactSection from '../components/ContactSection.jsx';
import Footer from '../components/Footer.jsx';
import Preloader from '../components/Preloader.jsx';
import { Wrench, ShieldCheck } from 'lucide-react';
---

<Layout title="Werkstatt & MFK Services | Schossacher Garage Schweiz">
  <Preloader client:load />
  <Navbar client:load />
  
  <main class="pt-28 pb-16 bg-[#07080A]">
    {/* Page Header */}
    <section class="py-16 relative bg-[#0E121D] border-b border-[#D4AF37]/20">
      <div class="container text-center max-w-3xl space-y-4">
        <div class="gold-badge mx-auto">
          <Wrench className="w-4 h-4 text-[#FFD700]" />
          <span>Schweizer Meisterbetrieb in Dübendorf</span>
        </div>
        <h1 class="text-4xl md:text-6xl font-bold text-white tracking-tight font-serif">
          Werkstatt & <span class="text-gold-gradient">MFK Services</span>
        </h1>
        <p class="text-stone-300 text-base md:text-lg font-semibold">
          Qualitätsgeprüfte Reparaturen, Wartungen nach Herstellervorgaben und 100% MFK-Vorführgarantie für alle Marken im Schossacher 22.
        </p>
      </div>
    </section>

    <Services client:visible />

    {/* Service Guarantee Highlight Banner */}
    <section class="py-16 bg-[#07080A] relative">
      <div class="container">
        <div class="glass-card-gold p-8 md:p-12 border-[#D4AF37]/40 text-center space-y-6 max-w-4xl mx-auto !bg-[#0E121D]">
          <div class="w-14 h-14 rounded-2xl bg-amber-500/10 border border-[#D4AF37] flex items-center justify-center text-[#FFD700] mx-auto">
            <ShieldCheck className="w-8 h-8" />
          </div>
          <h2 class="text-3xl font-bold text-white font-serif">Unsere Schweizer Service-Garantie</h2>
          <p class="text-stone-300 text-sm md:text-base max-w-2xl mx-auto font-semibold">
            Jede Arbeit in der Schossacher Garage beinhaltet 12 Monate Garantie auf Ersatzteile & Arbeit. Wir arbeiten streng nach Werksvorgaben – damit bleibt Ihre Neuwagen-Herstellergarantie vollständig erhalten.
          </p>
          <div class="pt-4 flex justify-center gap-4">
            <a href="/kontakt" class="btn-gold text-sm">
              Termin Buchen
            </a>
          </div>
        </div>
      </div>
    </section>

    <ContactSection client:visible />
  </main>

  <Footer client:visible />
</Layout>
